{
  "routes": [
    {
      "method": "GET",
      "path": "/project-works",
      "handler": "project-work.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/project-works/count",
      "handler": "project-work.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/project-works/:id",
      "handler": "project-work.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/project-works",
      "handler": "project-work.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/project-works/:id",
      "handler": "project-work.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/project-works/:id",
      "handler": "project-work.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
